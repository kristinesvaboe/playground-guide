services:
  db:
    image: postgis/postgis:17-3.5
    environment:
      POSTGRES_DB: playgroundguide
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
